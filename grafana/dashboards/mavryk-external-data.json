{
  "annotations": {
    "list": [
      {
        "builtIn": 1,
        "datasource": { "type": "grafana", "uid": "-- Grafana --" },
        "enable": true,
        "hide": true,
        "iconColor": "rgba(0, 211, 255, 1)",
        "name": "Annotations & Alerts",
        "type": "dashboard"
      }
    ]
  },
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 1,
  "links": [],
  "panels": [
    {
      "type": "text",
      "id": 1,
      "title": "About",
      "gridPos": { "h": 4, "w": 24, "x": 0, "y": 0 },
      "options": {
        "mode": "markdown",
        "content": "**Mavryk External Data** — observability dashboard.\n\nFive metric families in this board:\n- **HTTP (inbound)** — `http_*` from gin middleware.\n- **Outbound HTTP** — `outbound_http_*` from the resilience stack (CoinGecko + Equiteez). `outbound_http_requests_total{component,outcome}` counts every attempt (incl. retries); subtract `outbound_http_retries_total` to get logical-request RPS.\n- **Background jobs** — `job_*` and `backfill_*` from live, backfill, RWA, tickers collectors.\n- **FX conversions** — `fx_*` from RWA + tickers `?in=` paths.\n- **Tickers** — `tickers_*` for the CoinGecko per-exchange ingestion + per-endpoint cache hit ratios.\n- **Database pool** — `db_pool_*` sampled every 15s by `metrics.StartDBPoolCollector`.\n\nSet **namespace** to your scrape label (default: `mavryk-external-data`)."
      }
    },

    { "type": "row", "id": 2, "title": "HTTP (inbound)", "collapsed": false,
      "gridPos": { "h": 1, "w": 24, "x": 0, "y": 4 }, "panels": [] },

    {
      "type": "timeseries", "id": 3, "title": "HTTP RPS (total)",
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "gridPos": { "h": 6, "w": 24, "x": 0, "y": 5 },
      "fieldConfig": { "defaults": { "unit": "reqps" } },
      "targets": [{ "refId": "A",
        "expr": "sum(rate(http_requests_total{namespace=\"$namespace\"}[$__rate_interval]))",
        "legendFormat": "rps" }]
    },
    {
      "type": "timeseries", "id": 4, "title": "HTTP RPS by path",
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 11 },
      "fieldConfig": { "defaults": { "unit": "reqps" } },
      "targets": [{ "refId": "A",
        "expr": "sum by (path) (rate(http_requests_total{namespace=\"$namespace\"}[$__rate_interval]))",
        "legendFormat": "{{path}}" }]
    },
    {
      "type": "timeseries", "id": 5, "title": "HTTP latency p50 / p95 by path",
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 11 },
      "fieldConfig": { "defaults": { "unit": "s" } },
      "targets": [
        { "refId": "A",
          "expr": "histogram_quantile(0.50, sum by (path, le) (rate(http_request_duration_seconds_bucket{namespace=\"$namespace\"}[$__rate_interval])))",
          "legendFormat": "p50 {{path}}" },
        { "refId": "B",
          "expr": "histogram_quantile(0.95, sum by (path, le) (rate(http_request_duration_seconds_bucket{namespace=\"$namespace\"}[$__rate_interval])))",
          "legendFormat": "p95 {{path}}" }
      ]
    },
    {
      "type": "timeseries", "id": 6, "title": "HTTP responses by status class",
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 19 },
      "fieldConfig": { "defaults": { "unit": "reqps" } },
      "targets": [{ "refId": "A",
        "expr": "sum by (status_class) (rate(http_responses_total{namespace=\"$namespace\"}[$__rate_interval]))",
        "legendFormat": "{{status_class}}" }]
    },
    {
      "type": "timeseries", "id": 7, "title": "HTTP RPS by method+path",
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 19 },
      "fieldConfig": { "defaults": { "unit": "reqps" } },
      "targets": [{ "refId": "A",
        "expr": "sum by (method, path) (rate(http_requests_total{namespace=\"$namespace\"}[$__rate_interval]))",
        "legendFormat": "{{method}} {{path}}" }]
    },

    { "type": "row", "id": 10, "title": "Outbound HTTP (CoinGecko / Equiteez)",
      "collapsed": false, "gridPos": { "h": 1, "w": 24, "x": 0, "y": 27 }, "panels": [] },

    {
      "type": "stat", "id": 11, "title": "Circuit breaker state per component",
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "gridPos": { "h": 6, "w": 12, "x": 0, "y": 28 },
      "fieldConfig": {
        "defaults": {
          "mappings": [
            { "type": "value", "options": {
              "0": { "text": "closed",    "color": "green"  },
              "1": { "text": "OPEN",      "color": "red"    },
              "2": { "text": "half-open", "color": "yellow" }
            } }
          ],
          "thresholds": { "mode": "absolute", "steps": [
            { "color": "green",  "value": null },
            { "color": "red",    "value": 0.5 },
            { "color": "yellow", "value": 1.5 }
          ] }
        }
      },
      "options": {
        "reduceOptions": { "calcs": ["lastNotNull"], "fields": "", "values": false },
        "graphMode": "none",
        "colorMode": "background"
      },
      "targets": [{ "refId": "A",
        "expr": "outbound_http_circuit_breaker_state{namespace=\"$namespace\"}",
        "legendFormat": "{{component}}" }]
    },
    {
      "type": "timeseries", "id": 12, "title": "Outbound retries (rate by component)",
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "gridPos": { "h": 6, "w": 12, "x": 12, "y": 28 },
      "fieldConfig": { "defaults": { "unit": "short" } },
      "targets": [{ "refId": "A",
        "expr": "sum by (component) (rate(outbound_http_retries_total{namespace=\"$namespace\"}[$__rate_interval]))",
        "legendFormat": "{{component}}" }]
    },
    {
      "type": "timeseries", "id": 13, "title": "CB transitions (rate)",
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "gridPos": { "h": 6, "w": 12, "x": 0, "y": 34 },
      "fieldConfig": { "defaults": { "unit": "short" } },
      "targets": [{ "refId": "A",
        "expr": "sum by (component, from_state, to_state) (rate(outbound_http_circuit_breaker_transitions_total{namespace=\"$namespace\"}[$__rate_interval]))",
        "legendFormat": "{{component}} {{from_state}}→{{to_state}}" }]
    },
    {
      "type": "timeseries", "id": 14, "title": "Rate-limit wait p95 (s)",
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "gridPos": { "h": 6, "w": 12, "x": 12, "y": 34 },
      "fieldConfig": { "defaults": { "unit": "s" } },
      "targets": [{ "refId": "A",
        "expr": "histogram_quantile(0.95, sum by (component, le) (rate(outbound_http_rate_limit_wait_seconds_bucket{namespace=\"$namespace\"}[$__rate_interval])))",
        "legendFormat": "{{component}}" }]
    },
    {
      "type": "timeseries", "id": 15, "title": "Outbound RPS by component",
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "gridPos": { "h": 6, "w": 12, "x": 0, "y": 40 },
      "fieldConfig": {
        "defaults": {
          "unit": "reqps",
          "description": "Every attempt (including retries) counted once. Subtract outbound_http_retries_total to get logical-request RPS."
        }
      },
      "targets": [{ "refId": "A",
        "expr": "sum by (component) (rate(outbound_http_requests_total{namespace=\"$namespace\"}[$__rate_interval]))",
        "legendFormat": "{{component}}" }]
    },
    {
      "type": "timeseries", "id": 16, "title": "Outbound requests by outcome",
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "gridPos": { "h": 6, "w": 12, "x": 12, "y": 40 },
      "fieldConfig": {
        "defaults": {
          "unit": "reqps",
          "description": "Outcome: 2xx healthy, 4xx caller bug / rate-limited, 5xx upstream, error = transport failure (no HTTP status)."
        }
      },
      "targets": [{ "refId": "A",
        "expr": "sum by (component, outcome) (rate(outbound_http_requests_total{namespace=\"$namespace\"}[$__rate_interval]))",
        "legendFormat": "{{component}} {{outcome}}" }]
    },
    {
      "type": "stat", "id": 17, "title": "Outbound requests last 1h (by component)",
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "gridPos": { "h": 6, "w": 8, "x": 0, "y": 46 },
      "fieldConfig": {
        "defaults": {
          "unit": "short",
          "description": "Total outbound attempts in the last hour. Useful for capacity planning against CoinGecko's quota."
        }
      },
      "options": {
        "reduceOptions": { "calcs": ["lastNotNull"], "fields": "", "values": false },
        "graphMode": "area",
        "colorMode": "value"
      },
      "targets": [{ "refId": "A",
        "expr": "sum by (component) (increase(outbound_http_requests_total{namespace=\"$namespace\"}[1h]))",
        "legendFormat": "{{component}}" }]
    },
    {
      "type": "stat", "id": 19, "title": "CoinGecko requests last 24h (daily quota burn)",
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "gridPos": { "h": 6, "w": 8, "x": 8, "y": 46 },
      "fieldConfig": {
        "defaults": {
          "unit": "short",
          "description": "Total outbound attempts to CoinGecko over the last 24h. Thresholds reflect a 15k/day soft budget — adjust to your actual CG plan quota.",
          "thresholds": { "mode": "absolute", "steps": [
            { "color": "green",  "value": null },
            { "color": "yellow", "value": 10000 },
            { "color": "red",    "value": 14000 }
          ] }
        }
      },
      "options": {
        "reduceOptions": { "calcs": ["lastNotNull"], "fields": "", "values": false },
        "graphMode": "area",
        "colorMode": "value"
      },
      "targets": [{ "refId": "A",
        "expr": "sum (increase(outbound_http_requests_total{namespace=\"$namespace\",component=\"coingecko\"}[24h]))",
        "legendFormat": "coingecko 24h" }]
    },
    {
      "type": "timeseries", "id": 18, "title": "Outbound error ratio (% non-2xx + transport)",
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "gridPos": { "h": 6, "w": 8, "x": 16, "y": 46 },
      "fieldConfig": {
        "defaults": {
          "unit": "percentunit",
          "description": "Share of attempts that returned 4xx/5xx or failed at the transport layer. Sustained > 5% is alertable."
        }
      },
      "targets": [{ "refId": "A",
        "expr": "sum by (component) (rate(outbound_http_requests_total{namespace=\"$namespace\",outcome!=\"2xx\"}[$__rate_interval])) / clamp_min(sum by (component) (rate(outbound_http_requests_total{namespace=\"$namespace\"}[$__rate_interval])), 1e-9)",
        "legendFormat": "{{component}}" }]
    },

    { "type": "row", "id": 20, "title": "Background jobs (live / backfill / RWA / tickers)",
      "collapsed": false, "gridPos": { "h": 1, "w": 24, "x": 0, "y": 52 }, "panels": [] },

    {
      "type": "timeseries", "id": 21, "title": "Job tick duration p95 by (job, entity)",
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "gridPos": { "h": 8, "w": 24, "x": 0, "y": 53 },
      "fieldConfig": { "defaults": { "unit": "s" } },
      "targets": [{ "refId": "A",
        "expr": "histogram_quantile(0.95, sum by (job, source, entity, le) (rate(job_tick_duration_seconds_bucket{namespace=\"$namespace\"}[$__rate_interval])))",
        "legendFormat": "{{job}}/{{source}}/{{entity}}" }]
    },
    {
      "type": "timeseries", "id": 22, "title": "Job errors by reason (rate)",
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 61 },
      "fieldConfig": { "defaults": { "unit": "short" } },
      "targets": [{ "refId": "A",
        "expr": "sum by (job, source, reason) (rate(job_errors_total{namespace=\"$namespace\"}[$__rate_interval]))",
        "legendFormat": "{{job}}/{{source}} {{reason}}" }]
    },
    {
      "type": "timeseries", "id": 23, "title": "Rows affected (rate)",
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 61 },
      "fieldConfig": { "defaults": { "unit": "short" } },
      "targets": [{ "refId": "A",
        "expr": "sum by (job, source, entity) (rate(job_rows_affected_total{namespace=\"$namespace\"}[$__rate_interval]))",
        "legendFormat": "{{job}} {{entity}}" }]
    },

    { "type": "row", "id": 30, "title": "Backfill cursor",
      "collapsed": false, "gridPos": { "h": 1, "w": 24, "x": 0, "y": 69 }, "panels": [] },

    {
      "type": "timeseries", "id": 31, "title": "Oldest backfilled timestamp (cursor) per token",
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "gridPos": { "h": 8, "w": 24, "x": 0, "y": 70 },
      "fieldConfig": {
        "defaults": {
          "unit": "dateTimeAsIso",
          "description": "Decreases over time as the backfill walks history backward; flat = stalled or reached_floor."
        }
      },
      "targets": [{ "refId": "A",
        "expr": "backfill_oldest_ts_seconds{namespace=\"$namespace\"} * 1000",
        "legendFormat": "{{source}}/{{entity}}" }]
    },
    {
      "type": "timeseries", "id": 32, "title": "Auto-disabled events (rate)",
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "gridPos": { "h": 6, "w": 24, "x": 0, "y": 78 },
      "fieldConfig": { "defaults": { "unit": "short" } },
      "targets": [{ "refId": "A",
        "expr": "sum by (entity, reason) (increase(backfill_auto_disabled_total{namespace=\"$namespace\"}[1h]))",
        "legendFormat": "{{entity}} ({{reason}})" }]
    },

    { "type": "row", "id": 40, "title": "Database connection pool",
      "collapsed": false, "gridPos": { "h": 1, "w": 24, "x": 0, "y": 84 }, "panels": [] },

    {
      "type": "timeseries", "id": 41, "title": "DB pool connections (open / in_use / idle)",
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "gridPos": { "h": 8, "w": 16, "x": 0, "y": 85 },
      "fieldConfig": { "defaults": { "unit": "short" } },
      "targets": [
        { "refId": "A", "expr": "db_pool_open_connections{namespace=\"$namespace\"}",   "legendFormat": "open" },
        { "refId": "B", "expr": "db_pool_in_use_connections{namespace=\"$namespace\"}", "legendFormat": "in_use" },
        { "refId": "C", "expr": "db_pool_idle_connections{namespace=\"$namespace\"}",   "legendFormat": "idle" }
      ]
    },
    {
      "type": "stat", "id": 42, "title": "Open connections (current)",
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "gridPos": { "h": 8, "w": 8, "x": 16, "y": 85 },
      "fieldConfig": {
        "defaults": {
          "unit": "short",
          "thresholds": { "mode": "absolute", "steps": [
            { "color": "green",  "value": null },
            { "color": "yellow", "value": 18 },
            { "color": "red",    "value": 23 }
          ] }
        }
      },
      "options": {
        "reduceOptions": { "calcs": ["lastNotNull"], "fields": "", "values": false },
        "graphMode": "area",
        "colorMode": "value"
      },
      "targets": [{ "refId": "A",
        "expr": "db_pool_open_connections{namespace=\"$namespace\"}",
        "legendFormat": "open" }]
    },
    {
      "type": "timeseries", "id": 43, "title": "DB pool wait rate (s/s)",
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "gridPos": { "h": 6, "w": 24, "x": 0, "y": 93 },
      "fieldConfig": {
        "defaults": {
          "unit": "s",
          "description": "Saturation: seconds-per-second the pool spent blocking acquirers (rate of cumulative wait counter). Sustained > 0.1 s/s means the pool is undersized."
        }
      },
      "targets": [{ "refId": "A",
        "expr": "rate(db_pool_wait_duration_seconds{namespace=\"$namespace\"}[$__rate_interval])",
        "legendFormat": "wait/s" }]
    },

    { "type": "row", "id": 50, "title": "FX conversions (RWA + tickers `?in=`)",
      "collapsed": false, "gridPos": { "h": 1, "w": 24, "x": 0, "y": 99 }, "panels": [] },

    {
      "type": "timeseries", "id": 51, "title": "FX conversion p95 by (source_token, target)",
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 100 },
      "fieldConfig": {
        "defaults": {
          "unit": "s",
          "description": "p95 wall-time of one Convert() call (cache hit + miss combined)."
        }
      },
      "targets": [{ "refId": "A",
        "expr": "histogram_quantile(0.95, sum by (source_token, target, le) (rate(fx_conversion_duration_seconds_bucket{namespace=\"$namespace\"}[$__rate_interval])))",
        "legendFormat": "{{source_token}} → {{target}}" }]
    },
    {
      "type": "timeseries", "id": 52, "title": "FX conversion outcomes (rate by result)",
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 100 },
      "fieldConfig": {
        "defaults": {
          "unit": "short",
          "description": "Conversions per second labelled by result. `success`/`identity` are healthy; `no_rate`/`unregistered_source`/`query_error` indicate a problem."
        }
      },
      "options": {
        "legend": { "calcs": ["mean"], "displayMode": "table", "placement": "bottom", "showLegend": true }
      },
      "targets": [{ "refId": "A",
        "expr": "sum by (result) (rate(fx_conversions_total{namespace=\"$namespace\"}[$__rate_interval]))",
        "legendFormat": "{{result}}" }]
    },
    {
      "type": "timeseries", "id": 53, "title": "FX stale ratio per target (alertable)",
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "gridPos": { "h": 6, "w": 24, "x": 0, "y": 108 },
      "fieldConfig": {
        "defaults": {
          "unit": "percentunit",
          "description": "Share of conversions returned with `fx.stale=true` (FX older than `server.fx_max_staleness_seconds`). A growing ratio means the CoinGecko live job is behind. Alert on `> 1%` (0.01) sustained."
        }
      },
      "targets": [{ "refId": "A",
        "expr": "sum by (target) (rate(fx_stale_responses_total{namespace=\"$namespace\"}[$__rate_interval])) / clamp_min(sum by (target) (rate(fx_conversions_total{namespace=\"$namespace\"}[$__rate_interval])), 1e-9)",
        "legendFormat": "{{target}}" }]
    },

    { "type": "row", "id": 60, "title": "Tickers (CoinGecko per-exchange)", "collapsed": false,
      "gridPos": { "h": 1, "w": 24, "x": 0, "y": 114 }, "panels": [] },

    {
      "type": "timeseries", "id": 61, "title": "Active (exchange, target) pairs per token",
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "gridPos": { "h": 7, "w": 12, "x": 0, "y": 115 },
      "fieldConfig": {
        "defaults": {
          "unit": "short",
          "description": "Distinct (exchange, target_symbol) pairs in the latest CoinGecko tickers tick. Drops between ticks mean exchanges stopped reporting MVRK. Alert on `> 30% drop in 15m`."
        }
      },
      "targets": [{ "refId": "A",
        "expr": "tickers_active_count{namespace=\"$namespace\"}",
        "legendFormat": "{{source}}/{{token}}" }]
    },
    {
      "type": "timeseries", "id": 62, "title": "Distinct exchanges per token",
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "gridPos": { "h": 7, "w": 12, "x": 12, "y": 115 },
      "fieldConfig": {
        "defaults": {
          "unit": "short",
          "description": "Distinct exchanges reporting the token in the latest tick. Coarser than `tickers_active_count` — fires when an exchange disappears entirely (vs. dropping one of multiple pairs)."
        }
      },
      "targets": [{ "refId": "A",
        "expr": "tickers_exchanges_count{namespace=\"$namespace\"}",
        "legendFormat": "{{source}}/{{token}}" }]
    },
    {
      "type": "timeseries", "id": 63, "title": "Tickers job tick p95 (job=tickers)",
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "gridPos": { "h": 7, "w": 12, "x": 0, "y": 122 },
      "fieldConfig": {
        "defaults": {
          "unit": "s",
          "description": "p95 wall-time of one CoinGecko tickers job tick. Default cadence is 5m; ticks taking > 30s suggest CG /tickers is slow or returning a large payload (lots of exchanges)."
        }
      },
      "targets": [{ "refId": "A",
        "expr": "histogram_quantile(0.95, sum by (entity, le) (rate(job_tick_duration_seconds_bucket{namespace=\"$namespace\", job=\"tickers\"}[$__rate_interval])))",
        "legendFormat": "{{entity}}" }]
    },
    {
      "type": "timeseries", "id": 64, "title": "Tickers job errors by reason (rate)",
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "gridPos": { "h": 7, "w": 12, "x": 12, "y": 122 },
      "fieldConfig": {
        "defaults": {
          "unit": "short",
          "description": "Tickers job errors per second by reason. `fetch` = CoinGecko request failed (rate-limit / 5xx / network). `save` = DB write failed (FK / connection)."
        }
      },
      "targets": [{ "refId": "A",
        "expr": "sum by (reason) (rate(job_errors_total{namespace=\"$namespace\", job=\"tickers\"}[$__rate_interval]))",
        "legendFormat": "{{reason}}" }]
    },
    {
      "type": "timeseries", "id": 65, "title": "Tickers cache hit ratio (latest + distribution)",
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "gridPos": { "h": 7, "w": 12, "x": 0, "y": 129 },
      "fieldConfig": {
        "defaults": {
          "unit": "percentunit",
          "min": 0,
          "max": 1,
          "description": "Share of /v1/tickers requests served from the in-process cache. Default TTLs: latest=30s, distribution=60s. Healthy under regular polling is `> 0.5`; drops below mean clients are out-pacing the cache (or each request carries a unique `?in=` cardinality)."
        }
      },
      "targets": [{ "refId": "A",
        "expr": "sum by (endpoint) (rate(tickers_cache_requests_total{namespace=\"$namespace\", result=\"hit\"}[$__rate_interval])) / clamp_min(sum by (endpoint) (rate(tickers_cache_requests_total{namespace=\"$namespace\"}[$__rate_interval])), 1e-9)",
        "legendFormat": "{{endpoint}}" }]
    },
    {
      "type": "timeseries", "id": 66, "title": "Tickers HTTP p95 by endpoint",
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "gridPos": { "h": 7, "w": 12, "x": 12, "y": 129 },
      "fieldConfig": {
        "defaults": {
          "unit": "s",
          "description": "p95 inbound latency for /v1/tickers/* endpoints. Cache hits should be sub-1ms; spikes mean cache miss → SQL roundtrip (DISTINCT ON + LATERAL for /latest, GROUP BY for /distribution)."
        }
      },
      "targets": [{ "refId": "A",
        "expr": "histogram_quantile(0.95, sum by (path, le) (rate(http_request_duration_seconds_bucket{namespace=\"$namespace\", path=~\"/v1/tickers/.*\"}[$__rate_interval])))",
        "legendFormat": "{{path}}" }]
    },
    {
      "type": "row",
      "id": 67,
      "title": "Charts & /change queries + job panics",
      "collapsed": true,
      "gridPos": {
        "h": 1,
        "w": 24,
        "x": 0,
        "y": 136
      },
      "panels": [
        {
          "type": "timeseries",
          "id": 68,
          "title": "Job tick panics (recovered) /s",
          "datasource": {
            "type": "prometheus",
            "uid": "Prometheus"
          },
          "gridPos": {
            "h": 7,
            "w": 12,
            "x": 0,
            "y": 137
          },
          "fieldConfig": {
            "defaults": {
              "unit": "short"
            }
          },
          "targets": [
            {
              "refId": "A",
              "expr": "sum(rate(job_tick_panics_total{namespace=\"$namespace\"}[$__rate_interval])) by (job)",
              "legendFormat": "{{job}}"
            }
          ]
        },
        {
          "type": "timeseries",
          "id": 69,
          "title": "/change query p95 latency",
          "datasource": {
            "type": "prometheus",
            "uid": "Prometheus"
          },
          "gridPos": {
            "h": 7,
            "w": 12,
            "x": 12,
            "y": 137
          },
          "fieldConfig": {
            "defaults": {
              "unit": "s"
            }
          },
          "targets": [
            {
              "refId": "A",
              "expr": "histogram_quantile(0.95, sum(rate(change_query_duration_seconds_bucket{namespace=\"$namespace\"}[$__rate_interval])) by (le, kind))",
              "legendFormat": "p95 {{kind}}"
            }
          ]
        },
        {
          "type": "timeseries",
          "id": 70,
          "title": "/change cache outcomes /s",
          "datasource": {
            "type": "prometheus",
            "uid": "Prometheus"
          },
          "gridPos": {
            "h": 7,
            "w": 12,
            "x": 0,
            "y": 144
          },
          "fieldConfig": {
            "defaults": {
              "unit": "short"
            }
          },
          "targets": [
            {
              "refId": "A",
              "expr": "sum(rate(change_query_cache_hits_total{namespace=\"$namespace\"}[$__rate_interval])) by (result)",
              "legendFormat": "{{result}}"
            }
          ]
        },
        {
          "type": "timeseries",
          "id": 71,
          "title": "/change errors /s",
          "datasource": {
            "type": "prometheus",
            "uid": "Prometheus"
          },
          "gridPos": {
            "h": 7,
            "w": 12,
            "x": 12,
            "y": 144
          },
          "fieldConfig": {
            "defaults": {
              "unit": "short"
            }
          },
          "targets": [
            {
              "refId": "A",
              "expr": "sum(rate(change_query_errors_total{namespace=\"$namespace\"}[$__rate_interval])) by (code)",
              "legendFormat": "{{code}}"
            }
          ]
        },
        {
          "type": "timeseries",
          "id": 72,
          "title": "Chart query p95 latency",
          "datasource": {
            "type": "prometheus",
            "uid": "Prometheus"
          },
          "gridPos": {
            "h": 7,
            "w": 12,
            "x": 0,
            "y": 151
          },
          "fieldConfig": {
            "defaults": {
              "unit": "s"
            }
          },
          "targets": [
            {
              "refId": "A",
              "expr": "histogram_quantile(0.95, sum(rate(chart_query_duration_seconds_bucket{namespace=\"$namespace\"}[$__rate_interval])) by (le, kind, interval))",
              "legendFormat": "p95 {{kind}} {{interval}}"
            }
          ]
        },
        {
          "type": "timeseries",
          "id": 73,
          "title": "Chart rows returned (p95)",
          "datasource": {
            "type": "prometheus",
            "uid": "Prometheus"
          },
          "gridPos": {
            "h": 7,
            "w": 12,
            "x": 12,
            "y": 151
          },
          "fieldConfig": {
            "defaults": {
              "unit": "short"
            }
          },
          "targets": [
            {
              "refId": "A",
              "expr": "histogram_quantile(0.95, sum(rate(chart_query_rows_bucket{namespace=\"$namespace\"}[$__rate_interval])) by (le, kind, interval))",
              "legendFormat": "p95 {{kind}} {{interval}}"
            }
          ]
        },
        {
          "type": "timeseries",
          "id": 74,
          "title": "Chart cap hits /s",
          "datasource": {
            "type": "prometheus",
            "uid": "Prometheus"
          },
          "gridPos": {
            "h": 7,
            "w": 12,
            "x": 0,
            "y": 158
          },
          "fieldConfig": {
            "defaults": {
              "unit": "short"
            }
          },
          "targets": [
            {
              "refId": "A",
              "expr": "sum(rate(chart_query_cap_hits_total{namespace=\"$namespace\"}[$__rate_interval])) by (reason, interval)",
              "legendFormat": "{{reason}} {{interval}}"
            }
          ]
        }
      ]
    }
  ],
  "preload": false,
  "refresh": "30s",
  "schemaVersion": 40,
  "tags": ["mavryk", "external-data", "quotes", "prometheus"],
  "templating": {
    "list": [
      {
        "current": { "selected": true, "text": "mavryk-external-data", "value": "mavryk-external-data" },
        "hide": 0,
        "label": "Kubernetes namespace",
        "name": "namespace",
        "options": [
          { "selected": true, "text": "mavryk-external-data", "value": "mavryk-external-data" }
        ],
        "query": "mavryk-external-data",
        "skipUrlSync": false,
        "type": "custom"
      }
    ]
  },
  "time": { "from": "now-6h", "to": "now" },
  "timepicker": {},
  "timezone": "browser",
  "title": "Mavryk External Data (Quotes)",
  "uid": "mavryk-external-data-quotes",
  "version": 6,
  "weekStart": ""
}
